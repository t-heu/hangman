{
  "name": "backend",
  "description": "Periodically delete unused Firebase rooms",
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "ts-node-dev --respawn --transpile-only ."
  },
  "engines": {
    "node": "18"
  },
  "main": "src/index.ts",
  "dependencies": {
    "@google/generative-ai": "^0.14.1",
    "node-cron": "^3.0.3",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/node-cron": "^3.0.11",
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "eslint-plugin-import": "^2.25.4",
    "typescript": "^4.9.0"
  },
  "private": true
}
